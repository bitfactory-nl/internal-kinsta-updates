// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * LogService fetches Kinsta's per-environment log files and turns them into
 * something a human can act on: grouped, classified and with a verdict per group
 * on whether an AI fix attempt makes sense.
 * 
 * It is read-only towards Kinsta; the only thing it changes is its own cache.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as domain$0 from "../domain/models.js";

/**
 * Fetch retrieves one log file for an environment and returns it grouped.
 */
export function Fetch(projectID: string, envID: string, file: string, lines: number): $CancellablePromise<domain$0.LogFetchResult> {
    return $Call.ByID(4217327997, projectID, envID, file, lines).then(($result: any) => {
        return $$createType0($result);
    });
}

/**
 * FixPreview shows exactly what would be sent to the AI, so it can be read
 * before anything leaves the machine.
 */
export function FixPreview(projectID: string, groupID: string): $CancellablePromise<domain$0.AIFixPreview> {
    return $Call.ByID(3143523058, projectID, groupID).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * GroupByID returns a group from the last fetch for this project.
 */
export function GroupByID(projectID: string, groupID: string): $CancellablePromise<[domain$0.LogGroup, domain$0.LogFile]> {
    return $Call.ByID(517191294, projectID, groupID).then(($result: any) => {
        $result[0] = $$createType2($result[0]);
        return $result;
    });
}

// Private type creation functions
const $$createType0 = domain$0.LogFetchResult.createFrom;
const $$createType1 = domain$0.AIFixPreview.createFrom;
const $$createType2 = domain$0.LogGroup.createFrom;
